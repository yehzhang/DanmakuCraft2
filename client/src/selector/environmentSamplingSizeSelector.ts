import { Point } from '../data/point';

const environmentSamplingSizeSelector = () => environmentSamplingSize;

const environmentSamplingSize: Point = { x: 4000, y: 4000 };

export default environmentSamplingSizeSelector;
