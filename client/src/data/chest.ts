export const maxChestSpawnIntervalMs = 9 * 60 * 1000;

export const minChestSpawnIntervalMs = 5 * 60 * 1000;

export const maxInitialChestSpawnIntervalMs = __DEV__ ? 1000 : 6 * 60 * 1000;

export const minInitialChestSpawnIntervalMs = __DEV__ ? 0 : 3 * 60 * 1000;
