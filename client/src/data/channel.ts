/** A number ranging from 0 to 255. */
export type Channel255 = number;

/** A number ranging from 0 to 360. */
export type Channel360 = number;

/** A number ranging from 0 to 1. */
export type Channel1 = number;

export const maxChannel360Value = 360;

export const maxChannel255Value = 255;
